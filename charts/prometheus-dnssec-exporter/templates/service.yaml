apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-dnssec-exporter.fullname" . }}
  labels:
    {{- include "prometheus-dnssec-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9204
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-dnssec-exporter.selectorLabels" . | nindent 4 }}
